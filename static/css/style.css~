:root {
    --bb-purple: #24445C;
    --bb-green: #f10f0f;
    --bb-facebook: #1877f2;
  }
  
  body {
    background-color: #f8f9fa;
  }
  
  .navbar-custom {
    background-color: var(--bb-purple);
  }
  
  .navbar-custom .navbar-brand,
  .navbar-custom .nav-link,
  .navbar-custom .dropdown-toggle,
  .navbar-custom .dropdown-item {
    color: white !important;
  }
  
  .navbar-custom .nav-link i {
    margin-right: 6px;
  }
  
  .icon-phone i,
  .icon-whatsapp i {
    color: var(--bb-green);
  }
  
  .icon-facebook i {
    color: var(--bb-facebook);
  }
  
  .navbar-custom .nav-link:hover,
  .navbar-custom .dropdown-item:hover {
    color: #f0c8ff !important;
  }
  
  .navbar-custom .form-control {
    background-color: #fff;
    color: #03224C;
  }
  
  .dropdown-menu {
    background-color: var(--bb-purple);
  }
  
  .dropdown-menu .dropdown-item:hover {
    background-color: #5a3366;
  }
  
  .whatsapp-float {
    position: fixed;
    bottom: 20px;
    right: 20px;
    background-color: var(--bb-green);
    color: white;
    border-radius: 50%;
    padding: 12px 14px;
    font-size: 24px;
    z-index: 1050;
    display: none;
  }
  
  @media (max-width: 991.98px) {
    .whatsapp-float {
      display: block;
    }
  }
  
  /* Efecte și fonturi */
  

  .reveal-wrapper {
    overflow: hidden;
  }
  
  .reveal-image {
    transform: translateX(-100%);
    animation: slide-in 1.5s ease-out forwards;
  }
  
  @keyframes slide-in {
    to {
      transform: translateX(0%);
    }
  }
  
  .despre-noi {
    font-family: 'Merriweather', serif;
    font-size: 16px !important; 
  }
 
  .carousel-left {
    position: absolute;
    top: 10px;           /* poziția inițială */
    left: 30px;
    z-index: 10;
    width: auto;
    padding: 15px;
    margin: 15px;
    text-align: left;
    transform: translateY(120px); /* mută textul cu 2 cm (aprox. 60px) */
  }
  
    .carousel-text-wrapper {
      background-color: rgba(255, 255, 255, 0.7); /* fundal alb semitransparent */
      padding: 20px;
      max-width: 600px; /* să nu se întindă pe toată lățimea */
    }

    .carousel-title {
      font-family: 'Merriweather', serif;
      font-size: 28px;
      font-weight: bold;
      color: #202d45;
      text-shadow: 1px 1px 4px rgba(6, 0, 8, 0.7);
      margin-bottom: 10px;
      margin-left: 0;
    }

    .carousel-brand {
      font-size: 48px;
      font-family: 'Merriweather', serif;
      color: #202d45;
      font-weight: bold;
      text-shadow: 1px 1px 4px rgba(6, 0, 8, 0.7);
      margin-bottom: 10px;
    }    
  
  .stats-section {
    background-color: #24445c!important;
    padding: 3rem 1rem; /* echivalent cu py-5 */
  }
  
  .stats-section .stat-number {
    font-size: 2.5rem; /* echivalent cu display-6 */
    font-weight: 700;
    color: #f6f6f7; /* Bootstrap primary */
  }
  
  .stats-section .stat-label {
    color: #fffefe;
    margin: 0;
    font-weight: 500;
  }
  
  .despre-noi {
    font-family: 'Merriweather', serif;
    font-size: 22px;
    font-weight: normal;
    color: #24445C;
    margin-bottom: 10px;
    margin-left: 0;
  }
  .titlu-font {
    font-family: 'Merriweather', serif;
    font-size: 22px;
    font-weight: bold;
    color: #24445C;
    margin-bottom: 10px;
    margin-left: 0;
  }

  @media (max-width: 768px) {
    .carousel-left {
      left: 50%;
      transform: translateX(-50%);
    }
  }